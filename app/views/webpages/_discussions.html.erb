
<%@webpage.discussions.each do |d|%>
<hr>
  
  
<div class="discusitem">
  <b><%=d.user.username%></b><div class="followlk"> <%=followlk(d)%>:</div> <%= d.title%>   <div style="display: inline; float: right; margin-right: 40px;" class="count"><%= pluralize(d.comments.count,'comment')%></div><br />
  <%= d.content%><i><br />
<%=render :partial => 'comments/form', :locals => {:did => d.id}%>
  <div class="commentsection">
    <%d.comments.each do |c|%>
    <div class="commentitem">
      C: <%=c.content%>--by <%=c.user.username%><div class="followlk"><%=followlk(c)%></div>
    </div><!--commentitem div end-->
    <%end%>
  </div><!--commentsection div end-->
</div><!--discusitem div end-->

<%end%>

