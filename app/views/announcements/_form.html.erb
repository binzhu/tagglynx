<%= form_for(@announcement) do |f| %>
  <% if @announcement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@announcement.errors.count, "error") %> prohibited this announcement from being saved:</h2>

      <ul>
      <% @announcement.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  <div class="field">
    <%= f.label :enabled %><br />
    <%= f.number_field :enabled %>
  </div>
  <div class="field">
    <%= f.label :allow_dismiss %><br />
    <%= f.number_field :allow_dismiss %>
  </div>
  <div class="field">
    <%= f.label :sort %><br />
    <%= f.number_field :sort %>
  </div>
  <div class="field">
    <%= f.label :date_inserted %><br />
    <%= f.datetime_select :date_inserted %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
